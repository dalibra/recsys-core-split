Place raw interaction CSVs here when regenerating split variants.

Expected filenames match the dataset configs in `exps/runs/configs/dataset/`,
for example `Beauty.csv`, `Sports.csv`, and `Movielens-1m.csv`.
